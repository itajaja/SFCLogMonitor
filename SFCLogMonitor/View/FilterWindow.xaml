<Window x:Class="SFCLogMonitor.View.FilterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Filter" Height="374" Width="325" WindowStyle="ToolWindow" ResizeMode="NoResize">
    <Grid Margin="10,5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock TextWrapping="Wrap" Margin="0,5">Add keywords to filter which rows to display. Any row containg at least one keyword will be displayed</TextBlock>
        <CheckBox Grid.Row="1">Disable keyword filtering</CheckBox>
        <ListView Margin="0,5" Grid.Row="2"></ListView>
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition  />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox></TextBox>
            <Button Grid.Column="1" Padding="2" Margin="5,0,0,0">
                <Image Source="/SFCLogMonitor;component/Resources/action_add_16xLG.png"></Image>
            </Button>
        </Grid>
    </Grid>
</Window>